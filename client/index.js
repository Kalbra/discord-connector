'use strict';

const DiscordRPC = require('discord-rpc');

function onLoaded() {
    /*    const client = require('discord-rich-presence')('798305083833188372');

        const client =

        client.updatePresence({
            state: 'slithering',
            details: '🐍',
            startTimestamp: Date.now(),
            endTimestamp: Date.now() + 1337,
            largeImageKey: 'pp',
            smallImageKey: 'pp',
            instance: true,
        });*/

    console.log("Loaded")
}